<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kfq.fund.dao.IMemberDAO">
	<!-- select -->
	<select id="" >
	
	</select>
	
	<!-- insert -->
	<insert id="insertMember" parameterType="com.kfq.fund.vo.MemberVO">
		INSERT INTO MEMBER(EMAIL,PASSWD,USERCLASS,NICKNAME,USERKEY) 
		VALUES(#{email},#{passwd},#{userclass},#{nickname},#{userkey})
	</insert>
	<!-- update -->
	
	<!-- delete -->
</mapper>